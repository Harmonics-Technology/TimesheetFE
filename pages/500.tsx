import { InternalServerError } from '@components/bits-utils/InternalServerError';
import React from 'react';

const internalServerErro = () => {
    return <InternalServerError />;
};

export default internalServerErro;
