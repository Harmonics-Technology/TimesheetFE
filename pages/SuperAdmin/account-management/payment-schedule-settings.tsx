import { PayScheduleSettings } from '@components/subpages/PayScheduleSettings';
import React from 'react';

const PaySchedule = () => {
    return <PayScheduleSettings />;
};

export default PaySchedule;
